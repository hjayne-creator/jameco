:{$PORT:3000} {
	root * dist
	file_server

	@api {
		path /api/*
	}

	handle @api {
		uri strip_prefix /api
		reverse_proxy http://jameco-backend.railway.internal:8080
	}

	try_files {path} /index.html
}
