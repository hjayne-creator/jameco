Write the final PDP copy.

Use ONLY:
- The verified subject extract content
- Manufacturer-verified specs / features / certifications
- Gap rows where included=true

Do NOT use:
- Excluded gap rows
- Any price, stock, lead-time, shipping, tariff, promo, related products, accessories, substitutes, cross-sells
- Competitor names, citations, or research/audit language
- Vague superlatives ("best in class", "industry leading"), unsupported claims, or marketing fluff

Style + ordering rules:
- Produce content in EXACTLY this order: H1, identity_block, H2, overview, features, applications, specifications, faqs, customer_concerns. Omit a section if it has no verified content.
- H1: primary search phrase. Use brand + MPN + product type + key buyer-facing attribute.
- H2: secondary search phrase. Optimize slightly differently than H1 (e.g., reorder/swap one attribute).
- identity_block: bullet-style identifiers — Brand, Manufacturer, MPN, SKU, Series, etc. Only items with verified values.
- overview: one short paragraph (2-4 sentences) summarizing the most important verified product content.
- features: each item must include a benefit clearly traceable to a feature or spec. Skip items without traceable benefits.
- applications: short bullets, only if verified.
- specifications: list of {name, value, unit?}. The frontend renders this as a 2-column table where the second column shows value + unit.
- faqs: only if verified content exists.
- customer_concerns: only if recurring concerns appear in 2+ competitor PDPs OR are confirmed by the manufacturer (e.g., compatibility caveat).

Apply the JameCo style guide provided as additional context (if any). Keep language neutral, professional, and clear for both technical and non-technical buyers. Do not over-repeat the same fact.

Return JSON matching the FinalCopy schema.
