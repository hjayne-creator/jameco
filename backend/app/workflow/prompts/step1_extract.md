Extract structured product information from the subject product detail page below.

Capture verbatim where possible. Do NOT include any of the following in your output, even if present on the page:
- price, stock, lead time, shipping estimate, tariff status, promotions
- related products, accessories, substitutes, cross-sells, recommendations

If a field is not present on the page, leave it null or as an empty list. Do not invent values.

The page markdown is provided in the user message. Return JSON matching the schema strictly.
